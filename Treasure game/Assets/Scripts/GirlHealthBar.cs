using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GirlHealthBar : MonoBehaviour
{
    // [SerializeField] private float _maxHealth = 3;
    // [SerializeField] private GameObject _deathEffect, _hitEffect;
    // private float _currentHealth;
    

    // [SerializeField] private HealthBar _healthBar;

    // // Start is called before the first frame update
    // void Start()
    // {
    //     _currentHealth = _maxHealth;

    //     _healthBar.UpdateHealthBar(_maxHealth, _currentHealth);
    // }

    // // Update is called once per frame
    // void OnMouseDown() {
    //  _currentHealth -= Random.Range(0.5f, 1.5f);

    //  if (_currentHealth <= 0) {
    //     Instantiate(_deathEffect, transfrom.position, Quaternion.Euler(-90, 0, 0));
    //     Destroy(gameObject);
    //  }
    //  else {
    //     _healthBar.UpdateHealthBar(_maxHealth, _currentHealth);
    //     Instantiate(_hitEffect, transfrom.position, Quaternion.indentify);
    //  }
    // }
}
